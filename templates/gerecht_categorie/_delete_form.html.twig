<form method="post" action="{{ path('gerecht_categorie_delete', {'id': gerecht_categorie.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ gerecht_categorie.id) }}">
    <button class="btn">Delete</button>
</form>
