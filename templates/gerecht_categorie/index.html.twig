{% extends 'base.html.twig' %}

{% block title %}GerechtCategorie index{% endblock %}

{% block body %}
    <h1>GerechtCategorie index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Naam</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for gerecht_categorie in gerecht_categories %}
            <tr>
                <td>{{ gerecht_categorie.id }}</td>
                <td>{{ gerecht_categorie.Naam }}</td>
                <td>
                    <a href="{{ path('gerecht_categorie_show', {'id': gerecht_categorie.id}) }}">show</a>
                    <a href="{{ path('gerecht_categorie_edit', {'id': gerecht_categorie.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('gerecht_categorie_new') }}">Create new</a>
{% endblock %}
