{% extends 'base.html.twig' %}

{% block title %}GerechtCategorie{% endblock %}

{% block body %}
    <h1>GerechtCategorie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ gerecht_categorie.id }}</td>
            </tr>
            <tr>
                <th>Naam</th>
                <td>{{ gerecht_categorie.Naam }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('gerecht_categorie_index') }}">back to list</a>

    <a href="{{ path('gerecht_categorie_edit', {'id': gerecht_categorie.id}) }}">edit</a>

    {{ include('gerecht_categorie/_delete_form.html.twig') }}
{% endblock %}
