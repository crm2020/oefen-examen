<form method="post" action="{{ path('gerecht_soorten_delete', {'id': gerecht_soorten.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ gerecht_soorten.id) }}">
    <button class="btn">Delete</button>
</form>
