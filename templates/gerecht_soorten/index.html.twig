{% extends 'base.html.twig' %}

{% block title %}GerechtSoorten index{% endblock %}

{% block body %}
    <h1>GerechtSoorten index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Naam</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for gerecht_soorten in gerecht_soortens %}
            <tr>
                <td>{{ gerecht_soorten.id }}</td>
                <td>{{ gerecht_soorten.Naam }}</td>
                <td>
                    <a href="{{ path('gerecht_soorten_show', {'id': gerecht_soorten.id}) }}">show</a>
                    <a href="{{ path('gerecht_soorten_edit', {'id': gerecht_soorten.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('gerecht_soorten_new') }}">Create new</a>
{% endblock %}
