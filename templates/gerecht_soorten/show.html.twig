{% extends 'base.html.twig' %}

{% block title %}GerechtSoorten{% endblock %}

{% block body %}
    <h1>GerechtSoorten</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ gerecht_soorten.id }}</td>
            </tr>
            <tr>
                <th>Naam</th>
                <td>{{ gerecht_soorten.Naam }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('gerecht_soorten_index') }}">back to list</a>

    <a href="{{ path('gerecht_soorten_edit', {'id': gerecht_soorten.id}) }}">edit</a>

    {{ include('gerecht_soorten/_delete_form.html.twig') }}
{% endblock %}
