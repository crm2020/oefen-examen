<form method="post" action="{{ path('klanten_delete', {'id': klanten.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ klanten.id) }}">
    <button class="btn">Delete</button>
</form>
