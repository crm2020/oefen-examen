{% extends 'base.html.twig' %}

{% block title %}MenuItems{% endblock %}

{% block body %}
    <h1>MenuItems</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ menu_item.id }}</td>
            </tr>
            <tr>
                <th>Naam</th>
                <td>{{ menu_item.Naam }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('menu_items_index') }}">back to list</a>

    <a href="{{ path('menu_items_edit', {'id': menu_item.id}) }}">edit</a>

    {{ include('menu_items/_delete_form.html.twig') }}
{% endblock %}
